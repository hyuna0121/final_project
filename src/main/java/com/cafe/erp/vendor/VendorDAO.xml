<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.cafe.erp.vendor.VendorDAO">
    	<insert id="add" parameterType="VendorDTO">
    		insert into vendor(vendor_code, vendor_business_number, vendor_name, vendor_created_at, member_id)
    		values (#{vendorCode}, #{vendorBusinessNumber}, #{vendorName}, now(), #{memberId})
    	</insert>
    	
    	<select id="findAll" resultType="VendorDTO">
    		select * from vendor
    	</select>
    </mapper>